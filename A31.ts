const usernames: string[] = ['admin', 'john', 'wick', 'martin', 'nibba'];
if(usernames.length == 0){
  console.log(" We need to find some users!")}
else{
  for (let i=0; i< usernames.length ; i++ ) {
    usernames.slice(i);}
  console.log(" We need to find some users!")
}